# @package hydra
run:
    dir: /checkpoint/${oc.env:USER}/moco_v2_runs/${logging.name}/outputs/
sweep:
    dir: /checkpoint/${oc.env:USER}/moco_v2_runs/${logging.name}/outputs/
    subdir: ID${hydra.job.id} 